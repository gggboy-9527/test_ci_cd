/**app.js 一些通用的配置 */
export const appConfig = {
    title: 'My Vue Admin',
    theme: 'light', // 默认主题
    layout: 'side', // side | top | mix
    defaultLang: 'zh-CN',
    tokenKey: 'ACCESS_TOKEN' // LocalStorage 存储 token 的 key
}